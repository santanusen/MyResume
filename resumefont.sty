
% Needed to manage fonts
\RequirePackage[quiet]{fontspec}
% To support LaTeX quoting style
\defaultfontfeatures{Ligatures=TeX}
% Needed to manage math fonts
\RequirePackage{unicode-math}

%\usepackage[bitstream-charter]{mathdesign}
%\RequirePackage[T1]{fontenc}

% Needed to use icons from font-awesome
% (https://github.com/posquit0/latex-fontawesome)
\RequirePackage{fontawesome}

%-------------------------------------------------------------------------------
%                Configuration for directory locations
%-------------------------------------------------------------------------------
% Configure a directory location for fonts(default: 'fonts/')
\newcommand*{\fontdir}[1][fonts/]{\def\@fontdir{#1}}
\fontdir


%-------------------------------------------------------------------------------
%                Configuration for fonts
%-------------------------------------------------------------------------------
% Set the FontAwesome font to be up-to-date.
%\newfontfamily\FA[Path=\@fontdir]{FontAwesome}
%\setfontfamily\FA[Path=\@fontdir]{FontAwesome}
% Set font for header (default is Roboto)
\newfontfamily\roboto[
  Path=\@fontdir,
  UprightFont=*-Regular,
  ItalicFont=*-Italic,
  BoldFont=*-Bold,
  BoldItalicFont=*-BoldItalic,
]{Roboto}

\newfontfamily\robotolight[
  Path=\@fontdir,
  UprightFont=*-Thin,
  ItalicFont=*-ThinItalic,
  BoldFont=*-Medium,
  BoldItalicFont=*-MediumItalic,
]{Roboto}

%-------------------------------------------------------------------------------
%                sourcesanspro
%-------------------------------------------------------------------------------
%\RequirePackage[default,opentype]{sourcesanspro}
%\newcommand*{\resumefont}{\sourcesanspro}
%\newcommand*{\resumefontlight}{\sourcesansprolight}

%-------------------------------------------------------------------------------
%                sourceserifpro
%-------------------------------------------------------------------------------
\RequirePackage[default,opentype]{sourceserifpro}
%\newcommand*{\resumefont}{\sourceserifpro}
\newcommand*{\resumefontlight}{\sourceserifprolight}

%-------------------------------------------------------------------------------
%                Alegreya
%-------------------------------------------------------------------------------
%\RequirePackage[]{Alegreya}
%\newcommand*{\resumefont}{\Alegreya}
%\newcommand*{\resumefontlight}{\Alegreya}

%-------------------------------------------------------------------------------
%                DejaVu
%-------------------------------------------------------------------------------
%\RequirePackage[]{dejavu-otf}
%\newcommand*{\resumefont}{\DejaVuSerifCondensed}
%\newcommand*{\resumefontlight}{\DejaVuSerifCondensed}

%-------------------------------------------------------------------------------
%                EBGaramond
%-------------------------------------------------------------------------------
%\newfontfamily\garamond[
%  Path=/usr/share/fonts/opentype/ebgaramond/,
%  UprightFont=*-Regular,
%  ItalicFont=*-Italic,
%  BoldFont=*-Bold,
%  BoldItalicFont=*-Italic,
%]{EBGaramond12}
%
%\newfontfamily\garamondlight[
%  Path=/usr/share/fonts/opentype/ebgaramond/,
%  UprightFont=*-Regular,
%  ItalicFont=*-Italic,
%  BoldFont=*-Regular,
%  BoldItalicFont=*-Italic,
%]{EBGaramond08}
%
%\newcommand*{\resumefont}{\garamond}
%\newcommand*{\resumefontlight}{\garamondlight}

%-------------------------------------------------------------------------------
%                Georgia-Pro
%-------------------------------------------------------------------------------
%\newfontfamily\georgiapro[
%  Path=/usr/share/fonts/truetype/georgia-pro/,
%  UprightFont=*-Regular,
%  ItalicFont=*-Italic,
%  BoldFont=*-Bold,
%  BoldItalicFont=*-BoldItalic,
%]{GeorgiaPro}
%
%\newfontfamily\georgiaprolight[
%  Path=/usr/share/fonts/truetype/georgia-pro/,
%  UprightFont=*-Light,
%  ItalicFont=*-LightItalic,
%  BoldFont=*-SemiBold,
%  BoldItalicFont=*-SemiBoldItalic,
%]{GeorgiaPro}
%
%\newcommand*{\resumefont}{\georgiapro}
%\newcommand*{\resumefontlight}{\georgiaprolight}

%-------------------------------------------------------------------------------
%                Calibri
%-------------------------------------------------------------------------------
%\newfontfamily\calibri[
%  Path=/mnt/c/Windows/Fonts/,
%  UprightFont=*,
%  ItalicFont=*i,
%  BoldFont=*b,
%  BoldItalicFont=*z,
%]{calibri}
%
\newfontfamily\calibrilight[
  Path=/mnt/c/Windows/Fonts/,
  UprightFont=*l,
  ItalicFont=*li,
  BoldFont=*,
  BoldItalicFont=*i,
]{calibri}

%\newcommand*{\resumefont}{\calibri}
%\newcommand*{\resumefontlight}{\calibrilight}
\newcommand*{\resumedescrfontlight}{\calibrilight}


%-------------------------------------------------------------------------------
%                Cambria
%-------------------------------------------------------------------------------
\newfontfamily\cambria[
  Path=/mnt/c/Windows/Fonts/,
  UprightFont=*,
  ItalicFont=*i,
  BoldFont=*b,
  BoldItalicFont=*z,
]{cambria}

\newcommand*{\resumefont}{\cambria}
%\newcommand*{\resumefontlight}{\cambria}

%-------------------------------------------------------------------------------
%                Set Fonts
%-------------------------------------------------------------------------------
\newcommand*{\headerfont}{\resumefont}
\newcommand*{\headerfontlight}{\resumefontlight}
\newcommand*{\footerfont}{\resumefontlight}

\newcommand*{\titlefont}{\resumefont}
\newcommand*{\titlefontlight}{\resumefontlight}
\newcommand*{\descrfontlight}{\resumedescrfontlight}

